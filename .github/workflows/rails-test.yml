name: Rails Test Suite

on:
  workflow_call:
    inputs:
      ruby_version:
        description: 'Ruby version to use'
        required: false
        type: string
        default: '3.2.10'
      rails_env:
        description: 'RAILS_ENV value'
        required: false
        type: string
        default: 'test'
      node_version:
        description: 'Node.js version (for asset compilation)'
        required: false
        type: string
        default: '20'
      postgres_version:
        description: 'PostgreSQL version for service container'
        required: false
        type: string
        default: '16'
      bundler_cache:
        description: 'Enable bundler cache'
        required: false
        type: boolean
        default: true
      run_system_tests:
        description: 'Run system tests (requires Chrome)'
        required: false
        type: boolean
        default: true
      test_command:
        description: 'Custom test command (overrides default)'
        required: false
        type: string
        default: ''

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:${{ inputs.postgres_version }}
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      RAILS_ENV: ${{ inputs.rails_env }}
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test
      PGHOST: localhost
      PGUSER: postgres
      PGPASSWORD: postgres

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Ruby
        uses: ruby/setup-ruby@09a7688d3b55cf0e976497ff046b70949eeaccfd # v1
        with:
          ruby-version: ${{ inputs.ruby_version }}
          bundler-cache: ${{ inputs.bundler_cache }}

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ inputs.node_version }}

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpq-dev

      - name: Setup database
        run: |
          bin/rails db:create
          bin/rails db:schema:load

      - name: Precompile assets
        run: bin/rails assets:precompile
        continue-on-error: true

      - name: Run unit and integration tests
        if: ${{ inputs.test_command == '' }}
        run: bin/rails test

      - name: Run system tests
        if: ${{ inputs.run_system_tests && inputs.test_command == '' }}
        run: bin/rails test:system

      - name: Run custom test command
        if: ${{ inputs.test_command != '' }}
        env:
          TEST_COMMAND: ${{ inputs.test_command }}
        run: |
          if [[ "$TEST_COMMAND" =~ [\;\&\|\`\<\>\(\)\{\}] ]]; then
            echo "Unsafe test_command input rejected" >&2
            exit 1
          fi
          read -r -a TEST_ARGS <<< "$TEST_COMMAND"
          "${TEST_ARGS[@]}"

      - name: Upload screenshots on failure
        if: failure() && inputs.run_system_tests
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: system-test-screenshots
          path: tmp/screenshots/
          if-no-files-found: ignore
